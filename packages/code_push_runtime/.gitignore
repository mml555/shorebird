# Flutter/Dart build output. pubspec.lock IS tracked: this package is standalone
# (not a workspace member), so its lockfile is the record of what it resolved,
# same as code_push_server.
.dart_tool/
build/

# Generated by `flutter pub get` from the plugin dependency graph.
.flutter-plugins-dependencies
