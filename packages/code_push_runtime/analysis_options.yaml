# Standalone package (own pubspec.lock, not a workspace member), but it follows
# the monorepo's very_good lint set since it is written fresh rather than
# vendored.
include: package:very_good_analysis/analysis_options.10.0.0.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
