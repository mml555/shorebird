# Declares the opt-in `integration` tag used by test/integration/. Those tests
# require a live server + Postgres + MinIO and are skipped unless INTEGRATION=1
# and the required env vars are set. Run them with: dart test --tags integration
tags:
  integration:
    skip: "opt-in: set INTEGRATION=1 and DATABASE_URL/S3_ENDPOINT/BASE_URL"
