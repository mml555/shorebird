================================================================================
STRUCTURAL BODY CENSUS -- NOT PATCH DEMAND
================================================================================
Each existing method counts once, regardless of how likely that method is to
change in practice.

Percentages measure current Route B refusal reachability over methods that
EXIST. They do NOT estimate:
  - patch success rate,
  - frequency of real changes,
  - user demand,
  - or percentage of production fixes Route B can ship.

Only the LOWERING stage was run. Reachability, release retention, the
producer's own source-text refusals and the bytecode compiler are separate
stages and were not evaluated. `lowerable` means this stage raised no
objection, and nothing more.

The two corpora are reported separately and are NOT pooled: one is a synthetic
regression fixture written to exercise this mechanism, the other is an
application written by people who had never heard of it.
================================================================================

--------------------------------------------------------------------------------
airgap_app   (synthetic regression fixture)
--------------------------------------------------------------------------------
  dill                        /private/tmp/claude-501/-Users-mendell-shorebird/8688bc8f-b3b0-49e5-b8b9-c4f9ab0a7854/scratchpad/d0/census2/airgap.dill
  include                     package:airgap_probe/

  instance procedures considered       19
    lowerable now                      18    94.74%
    blocked by >=1 reason               1     5.26%

  excluded from the denominator, counted so the exclusion is visible:
    static (no receiver to lower)       0
    abstract / external (no body)       2
    no usable source span               0

  REFUSAL CATEGORIES
  category                          methods % of all     SOLE  % unlock
  --------------------------------------------------------------------------
  receiver.super_call                     1    5.26%        0     0.00%
  receiver.unconsumed_this                1    5.26%        0     0.00%

  SOLE = methods for which this is the ONLY refusal. "% unlock" is that
  count over the BLOCKED population: what closing this one category alone
  would move. Categories overlap, so the "methods" column does not sum to
  the blocked total and the "SOLE" column does not sum to it either --
  the remainder is methods blocked by two or more categories at once.

  INSIDE receiver.unconsumed_this -- by the PARENT of each `this`
  kernel parent node                 occurrences sole-blocked
  ------------------------------------------------------------
  SuperMethodInvocation                       1            0

  InstanceTearOff is `onPressed: _handleTap` -- `this` is the RECEIVER
  of a tear-off, and the lowered spelling is the same textual edit as a
  read. Arguments/VariableDeclaration/ReturnStatement are the receiver
  genuinely escaping. Raw node names, so an unexamined shape cannot hide.

  CONSTRUCTS, attributed once each  <-- PLAN FROM THIS TABLE
  construct                         methods % of all     SOLE  % unlock
  --------------------------------------------------------------------------
  construct.super                         1    5.26%        1   100.00%

  TOP BLOCKER CO-OCCURRENCES
       1   receiver.super_call + receiver.unconsumed_this

  These are why raw prevalence misleads: a pair that co-occurs often is a
  pair where closing either one alone unlocks almost nothing.

  D-HYGIENE EXERCISE COUNT (informational -- NOT a blocker)
    declarations spelling `self`        1 of 19 with readable source
      of those, lowerable now           1
    These lower CORRECTLY; they cost a capture-avoiding receiver rename.
    Counting them as unsupported would put a solved problem back into the
    ranking. The test is the producer's own conservative substring scan, so
    it over-triggers on comments and on names like `selfTest`.

--------------------------------------------------------------------------------
Wonderous   (real application)
--------------------------------------------------------------------------------
  dill                        /private/tmp/claude-501/-Users-mendell-shorebird/8688bc8f-b3b0-49e5-b8b9-c4f9ab0a7854/scratchpad/d0/census2/wonderous.dill
  include                     package:wonders/

  instance procedures considered     1282
    lowerable now                    1179    91.97%
    blocked by >=1 reason             103     8.03%

  excluded from the denominator, counted so the exclusion is visible:
    static (no receiver to lower)      44
    abstract / external (no body)     355
    no usable source span               6

  REFUSAL CATEGORIES
  category                          methods % of all     SOLE  % unlock
  --------------------------------------------------------------------------
  receiver.unconsumed_this               91    7.10%       46    44.66%
  abi.named_parameters                    8    0.62%        7     6.80%
  abi.generic                             2    0.16%        2     1.94%
  receiver.compound_same_offset           2    0.16%        2     1.94%
  abi.optional_positionals                1    0.08%        1     0.97%
  receiver.super_call                    43    3.35%        0     0.00%
  receiver.super_get                      1    0.08%        0     0.00%

  SOLE = methods for which this is the ONLY refusal. "% unlock" is that
  count over the BLOCKED population: what closing this one category alone
  would move. Categories overlap, so the "methods" column does not sum to
  the blocked total and the "SOLE" column does not sum to it either --
  the remainder is methods blocked by two or more categories at once.

  INSIDE receiver.unconsumed_this -- by the PARENT of each `this`
  kernel parent node                 occurrences sole-blocked
  ------------------------------------------------------------
  InstanceTearOff                            89           45
  SuperMethodInvocation                      43            0
  Arguments                                   2            2
  SuperPropertyGet                            1            0

  InstanceTearOff is `onPressed: _handleTap` -- `this` is the RECEIVER
  of a tear-off, and the lowered spelling is the same textual edit as a
  read. Arguments/VariableDeclaration/ReturnStatement are the receiver
  genuinely escaping. Raw node names, so an unexamined shape cannot hide.

  CONSTRUCTS, attributed once each  <-- PLAN FROM THIS TABLE
  construct                         methods % of all     SOLE  % unlock
  --------------------------------------------------------------------------
  construct.method_tearoff               53    4.13%       44    42.72%
  construct.super                        44    3.43%       37    35.92%
  abi.named_parameters                    8    0.62%        7     6.80%
  abi.generic                             2    0.16%        2     1.94%
  receiver.compound_same_offset           2    0.16%        2     1.94%
  construct.this_escape                   2    0.16%        1     0.97%
  abi.optional_positionals                1    0.08%        1     0.97%

  TOP BLOCKER CO-OCCURRENCES
      43   receiver.super_call + receiver.unconsumed_this
       1   receiver.super_get + receiver.unconsumed_this
       1   abi.named_parameters + receiver.unconsumed_this

  These are why raw prevalence misleads: a pair that co-occurs often is a
  pair where closing either one alone unlocks almost nothing.

  D-HYGIENE EXERCISE COUNT (informational -- NOT a blocker)
    declarations spelling `self`        9 of 1282 with readable source
      of those, lowerable now           7
    These lower CORRECTLY; they cost a capture-avoiding receiver rename.
    Counting them as unsupported would put a solved problem back into the
    ranking. The test is the producer's own conservative substring scan, so
    it over-triggers on comments and on names like `selfTest`.
