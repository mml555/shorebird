# SEMANTIC-MAP-1 — FINAL

<!-- GENERATED by final/lib/gen_result.py. Do not edit: every number here
     is read from evidence/final_matrix.json, evidence/final_verdict.json
     and evidence/provenance_manifest.json at write time. Editing this
     file creates a second copy of the numbers, free to drift from the
     evidence -- which already happened in this programme once. -->

Generated 2026-09-09T07:10:30Z · issue #58 · tracker #48

## Verdict

    MODIFY_MAP_DESIGN

Selected by the predicate `DISTINCTION_NOT_REPRESENTABLE`, applying #58's precedence.

The verdict is **computed from the matrix**, not declared beside it. Every predicate is published below, not only the one that selected, so the decision surface is visible and the outcome can be checked for having been reachable any other way.

| verdict | predicate | value | selected |
| --- | --- | --- | --- |
| `MODIFY_ANALYZER` | `ANALYZER_DEFECT_UNDER_VALID_MODEL` | False | no |
| `MODIFY_MAP_DESIGN` | `DISTINCTION_NOT_REPRESENTABLE` | True | **yes** |
| `REDUCE_SCOPE` | `DECIDABLE_EXCLUSION_YIELDS_NONEMPTY` | False | no |
| `ABANDON_OR_REDESIGN` | `UNSAFE_CLASS_NOT_IDENTIFIABLE` | False | no |
| `PROCEED` | `PROCEED_CONDITIONS_MET` | False | no |

## The matrix

Every row is extracted at run time from a named file and field or marker. A row whose evidence cannot be resolved reads `NOT_ESTABLISHED` and is never filled from a remembered conclusion.

| row | category | evidence |
| --- | --- | --- |
| `DECLARATION_ID_STABILITY` | `ESTABLISHED` | `g1_identity/evidence/g1_identity.json`, `g2_fingerprints/evidence/g2_fingerprints.json` |
| `DECLARATION_ID_COLLISIONS` | `ESTABLISHED` | `g1_identity/evidence/g1_identity.json` |
| `ABI_FINGERPRINT` | `ESTABLISHED` | `g2_fingerprints/evidence/g2_fingerprints.json` |
| `BODY_FINGERPRINT` | `ESTABLISHED` | `g2_fingerprints/evidence/g2_fingerprints.json` |
| `CLASSIFICATION_UNCHANGED` | `ESTABLISHED` | `g2_fingerprints/evidence/g2_fingerprints.json` |
| `CLASSIFICATION_CHANGED_BODY` | `ESTABLISHED` | `g2_fingerprints/evidence/g2_fingerprints.json` |
| `CLASSIFICATION_ABI_BREAK` | `ESTABLISHED` | `g2_fingerprints/evidence/g2_fingerprints.json` |
| `PRIVACY_DOMAIN_DERIVATION` | `ESTABLISHED` | `g3_privacy/evidence/g3_privacy.json` |
| `PRIVACY_CROSS_DOMAIN_REFUSAL` | `ESTABLISHED` | `g3_privacy/evidence/falsification.txt`, `g3_privacy/evidence/g3_privacy.json` |
| `RETENTION_CONTRACT` | `ESTABLISHED_WITH_LIMITS` | `g4_retention/evidence/g4_retention.json` |
| `RETENTION_WITHHELD_FAILS_CLOSED` | `ESTABLISHED_NEGATIVE` | `g4_retention/evidence/g4_retention.json` |
| `PATCHABILITY_SUBSET_PROPERTY` | `VACUOUS` | `g5_patchability/evidence/inlining_state.json`, `g5_patchability/evidence/subset.json` |
| `PATCHABILITY_DISTINCTION_REPRESENTABLE` | `NOT_REPRESENTABLE` | `g5_patchability/evidence/dispatch_experiment.txt` |
| `DECIDABLE_EXCLUSIONS` | `NAMED_AND_FAIL_CLOSED` | `g2_fingerprints/evidence/g2_fingerprints.json`, `g3_privacy/evidence/g3_privacy.json` |
| `OVER_CLAIM` | `REFUSAL_ENFORCED` | `g5_patchability/evidence/corpora.txt`, `g5_patchability/evidence/subset_check.txt` |
| `MAP_RELEASE_BINDING` | `ESTABLISHED` | `g6_binding/evidence/g6_binding.txt`, `g6_binding/evidence/map_probe.json`, `g6_binding/evidence/verify_probe.json` |
| `MAP_SCHEMA_VERSIONING` | `ESTABLISHED` | `g6_binding/evidence/g6_binding.txt`, `g6_binding/evidence/map_probe.json` |
| `MAP_SIZE` | `MEASURED_DETERMINISTIC` | `g7_cost/evidence/families.json` |
| `GENERATION_TIME` | `MEASURED_CURRENT_RUN_ONLY` | `g7_cost/evidence/families.json` |
| `RETENTION_COST_AT_SCALE` | `MEASURED_DETERMINISTIC_PROJECTION_LOW` | `g7_cost/evidence/families.json` |
| `REPRODUCIBILITY` | `ESTABLISHED` | `g8_reproduction/evidence/g8_result.json` |
| `KNOWN_GAPS` | `ENUMERATED` | `g8_reproduction/evidence/g8_result.json` |

No row is `NOT_ESTABLISHED`: every declared evidence pointer resolved.

### What each non-positive category means here

**`RETENTION_CONTRACT` — `ESTABLISHED_WITH_LIMITS`**  
The measurement is verified, but enforcement is PARTIAL: G4 carried that limitation forward rather than closing it.

**`RETENTION_WITHHELD_FAILS_CLOSED` — `ESTABLISHED_NEGATIVE`**  
MEASURED AND DOES NOT HOLD. Withholding some classes does not fail closed at load. This is a reproduced negative result, not a gap in the evidence, and it is a direct input to routing.

**`PATCHABILITY_SUBSET_PROPERTY` — `VACUOUS`**  
The subset property is satisfied because the admitted set is EMPTY. Zero admitted is not positive patchability evidence: no declaration was mechanically established safely patchable. NOT_INLINED and pool-indirect observations remain non-safety facts.

**`GENERATION_TIME` — `MEASURED_CURRENT_RUN_ONLY`**  
TIMING FAMILY, deliberately kept separate from the deterministic figures. These values are current-run observations and are not reproducible across runs. The order-dependence label set moves between identical runs even with the noise gate, so the max(stdev) classifier is a COST DIAGNOSTIC only -- never a safety or causality claim.

**`RETENTION_COST_AT_SCALE` — `MEASURED_DETERMINISTIC_PROJECTION_LOW`**  
Artifact-derived and reproducible. G4's retention curve UNDERSTATES the measured cost, so the projection may not be substituted for a measurement at the scale being shipped.

## Measurements

Deterministic and timing families are kept separate. The deterministic figures are artifact-derived and identical across runs; the timing figures are current-run observations and are not reproducible.

| figure | value | family |
| --- | --- | --- |
| declarations in cost scope | 35,139 | deterministic |
| map bytes | 9,467,041 | deterministic |
| release AOT bytes | 49,662,560 | deterministic |
| map / AOT | 19.063 % | deterministic |
| retention cost measured | 4,330,120 bytes (8.719 %) | deterministic |
| G4 projection | 2,922,310 bytes | deterministic |
| measured / projected | 1.482 × | deterministic |
| timed stages | 15 | **current run only** |
| timing samples | 60 (0 failed) | **current run only** |
| order-dependent labels | 0 | **current run only** |

## Known gaps

16 entries, generated from the matrix categories, the G8 production prerequisites and the G8 reproduced FAIL_OPEN findings. `UNKNOWN`, `VACUOUS`, `NOT_ESTABLISHED` and unresolved prerequisites appear verbatim.

| source | id | category | detail |
| --- | --- | --- | --- |
| matrix | `RETENTION_CONTRACT` | `ESTABLISHED_WITH_LIMITS` | The measurement is verified, but enforcement is PARTIAL: G4 carried that limitation forward rather than closing it. |
| matrix | `RETENTION_WITHHELD_FAILS_CLOSED` | `ESTABLISHED_NEGATIVE` | MEASURED AND DOES NOT HOLD. Withholding some classes does not fail closed at load. This is a reproduced negative result, not a gap in the evidence, and it is a direct input to routing. |
| matrix | `PATCHABILITY_SUBSET_PROPERTY` | `VACUOUS` | The subset property is satisfied because the admitted set is EMPTY. Zero admitted is not positive patchability evidence: no declaration was mechanically established safely patchable. NOT_INLINED and pool-indirect observations remain non-safety facts. |
| matrix | `PATCHABILITY_DISTINCTION_REPRESENTABLE` | `NOT_REPRESENTABLE` | The patch ATTACHED -- the C++ invoke of the target returned PATCHED-w -- and in the same run the direct call read PATCHED-w while the virtual call still read OLD-w. One declaration is therefore patched and not-observed simultaneously, depending on the call site, so "patchable" is not a property the map can carry per declaration. The dispatch predicate fails closed rather than classifying, which is the model declining to represent a distinction it cannot make. |
| matrix | `DECIDABLE_EXCLUSIONS` | `NAMED_AND_FAIL_CLOSED` | Each excluded class is NAMED in the map contract and its refusal is REACHED, not merely representable: the type-parameter shape is refused, an unsupported body refuses on either side of the comparison, and every privacy refusal category in the return surface is exercised. A refusal that exists in the code but is never reached would leave the exclusion undecidable in practice. |
| matrix | `GENERATION_TIME` | `MEASURED_CURRENT_RUN_ONLY` | TIMING FAMILY, deliberately kept separate from the deterministic figures. These values are current-run observations and are not reproducible across runs. The order-dependence label set moves between identical runs even with the noise gate, so the max(stdev) classifier is a COST DIAGNOSTIC only -- never a safety or causality claim. |
| matrix | `RETENTION_COST_AT_SCALE` | `MEASURED_DETERMINISTIC_PROJECTION_LOW` | Artifact-derived and reproducible. G4's retention curve UNDERSTATES the measured cost, so the projection may not be substituted for a measurement at the scale being shipped. |
| g8_prerequisite | `NON_STATIC_DISPATCH_PATCHABILITY` | `UNRESOLVED` | A patch must be observable at every call site that reaches the patched member, including virtual and instance dispatch. |
| g8_prerequisite | `POST_HOC_REDIRECTABILITY_UNDECIDABLE` | `UNRESOLVED` | Deciding whether a compiled call site is redirectable must not depend on reading tagged entry-point offsets back out of a finished snapshot. |
| g8_prerequisite | `INLINING_EVIDENCE_MUST_COME_FROM_THE_SHIPPED_RELEASE` | `UNRESOLVED` | The Route-2 inlining note a release consumes must have been produced by that exact release, proven by digest, not by build recipe. |
| g8_prerequisite | `COST_FIGURES_DO_NOT_EXTRAPOLATE` | `CARRY_FORWARD` | No cost decision may be made from a projection or from fixture-scale measurement; the figure must come from the scale being shipped. |
| g8_prerequisite | `ORDER_DEPENDENCE_IS_A_COST_DIAGNOSTIC_ONLY` | `CARRY_FORWARD` | The max(stdev(A), stdev(B)) order-dependence rule may inform cost diagnosis and must not be cited as a safety or causality claim. |
| g8_prerequisite | `MUTABLE_AOT_GATED_ON_SEMANTIC_MAP_FINAL` | `CARRY_FORWARD` | No Mutable-AOT implementation work may begin before SEMANTIC-MAP FINAL. |
| g8_fail_open | `subset-over-claim-arms` | `REPRODUCED` | the scorer refuses a predicted-patchable declaration that is not demonstrated: one partly stale, one wholly stale, one with no demonstration row |
| g8_fail_open | `corpora-over-claim-marker` | `REPRODUCED` | an injected admitted row is refused on each real corpus |
| g8_fail_open | `reader-control-defects` | `REPRODUCED` | both weakened readers fail the arms, so the suite is not vacuous |

## Non-proven claims, stated exactly

- No declaration was mechanically established safely patchable. The subset property is VACUOUS with 0 of 36 admitted.
- NOT_INLINED means no covered body-copy mechanism was witnessed. It does not mean every call site observes a patch: the isolated experiment attached a patch whose direct call read PATCHED-w while the virtual call still read OLD-w.
- Pool-indirect and entry-point observations are non-safety facts. Tagged offsets alias between the Function and Code layouts, so redirectability is not decidable from a finished snapshot.
- REPRODUCTION: PASS states that the evidence and its refusals rebuild. It is not a statement of production readiness; PRODUCTION_PREREQUISITES is a separate partition and remains UNRESOLVED.
- The full AOT SHA-256 is the authoritative release identity. The GNU build ID is diagnostic only -- build ids hash four snapshot segments and collide across different AOTs.
- Timing figures and the order-dependence label set are current-run observations. The max(stdev) classifier is a cost diagnostic, not causality or safety evidence.
- G4's retention curve understates measured cost at scale, so the projection may not be substituted for a measurement.

## Next lane

- The map cannot decide patchability by inference from a finished AOT, so patchability has to become a BUILD-TIME guarantee rather than a post-hoc classification. #59 (Mutable AOT Dart) is the accepted direction for that and may be named here; no implementation begins in #58.
- AOT-ASSUMPTIONS-1 owns the optimizer-contract half and is still un-instrumented; it was deliberately not started here.
- RETENTION_WITHHELD_FAILS_CLOSED is a measured NEGATIVE: withholding some retention classes does not fail closed at load. That is independent of the patchability question and needs its own lane before any map is relied on.
- Generation-time figures are current-run observations. Any schedule or budget derived from them needs a repeated measurement at the scale being shipped, not this sample.

## Provenance

15 evidence files consumed, each digested at read time; 47 producing scripts and 7 assembler components digested.

Input set derived from the extractor access log, cross-checked against the registry. Declared by rows: 15; opened by the extractor: 15; sets equal: True.

Self-hashing: none: this manifest, RESULT.md, the matrix and the verdict are OUTPUTS of this assembly and are listed by name only. A manifest that digested itself would never converge -- G5 hit exactly that and a second run never reproduced the first.

Manifest findings: none.

| evidence | sha256 | rows |
| --- | --- | --- |
| `g1_identity/evidence/g1_identity.json` | `8ffe2d18c684b15f…` | 2 |
| `g2_fingerprints/evidence/g2_fingerprints.json` | `e8e479a0c5cc9c6f…` | 7 |
| `g3_privacy/evidence/g3_privacy.json` | `870fc92b8e664fd7…` | 3 |
| `g3_privacy/evidence/falsification.txt` | `01fef376ea1139de…` | 1 |
| `g4_retention/evidence/g4_retention.json` | `dd9863b89fe33ed6…` | 2 |
| `g5_patchability/evidence/subset.json` | `21b644dcd142f3e9…` | 1 |
| `g5_patchability/evidence/inlining_state.json` | `8e97a2d235486e85…` | 1 |
| `g5_patchability/evidence/dispatch_experiment.txt` | `550908425469c359…` | 1 |
| `g5_patchability/evidence/subset_check.txt` | `c33c332441828b68…` | 1 |
| `g5_patchability/evidence/corpora.txt` | `e866a73f53a7d160…` | 1 |
| `g6_binding/evidence/verify_probe.json` | `9404d39d1a6200f5…` | 1 |
| `g6_binding/evidence/map_probe.json` | `53b1357b605ace0a…` | 2 |
| `g6_binding/evidence/g6_binding.txt` | `ed0aeaf37bbb6aef…` | 2 |
| `g7_cost/evidence/families.json` | `b948bd1f896e824a…` | 3 |
| `g8_reproduction/evidence/g8_result.json` | `6cc01362b2175504…` | 2 |

## Scope boundary

This lane extracted and classified. It implemented nothing: no patch-v2 format, no CLI integration, no signing change, no supported-state change, no physical deployment, and no `AOT-ASSUMPTIONS-1` work. No Mutable-AOT implementation began here.
