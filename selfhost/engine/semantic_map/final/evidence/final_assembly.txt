SM1-FINAL -- evidence matrix, computed verdict, provenance
generated 2026-09-09T08:00:52Z by run_final.sh
source tree /Users/mendell/shorebird/selfhost/engine/semantic_map

############ 0. SOURCE EVIDENCE FOR THE ROUTING MECHANISM ############
  FINAL published five source-level routing facts as hard-coded prose. The
  facts were correct, which is beside the point: a material routing conclusion
  outside the digested evidence graph cannot be falsified by Family A, is not
  in the provenance manifest, and does not degrade when its evidence goes
  away. They are produced here instead, read from the FROZEN tree via
  `git show <effective_tree>:<path>` rather than the working copy, each with
  its git blob id.

  holds      D2B_REGISTERS_VALIDATE_OPTION
             effective_tree:pkg/dart2bytecode/lib/dart2bytecode.dart:131  (1 match(es), expect_present=True)
  holds      D2B_ASSIGNS_DYNAMIC_INTERFACE_URI
             effective_tree:pkg/dart2bytecode/lib/dart2bytecode.dart:291  (1 match(es), expect_present=True)
  holds      VALIDATION_IS_CONDITIONAL_ON_URI
             effective_tree:pkg/front_end/lib/src/kernel/kernel_target.dart:1815  (1 match(es), expect_present=True)
  holds      OPTION_PREDATES_THE_FORK
             deps_pinned_base:pkg/dart2bytecode/lib/dart2bytecode.dart:121  (1 match(es), expect_present=True)
  holds      ROUTE_B_4A_OMITS_VALIDATE
             worktree:selfhost/engine/route_b/build_4a_payload.sh  (0 match(es), expect_present=False)
  holds      ROUTE_B_4B_OMITS_VALIDATE
             worktree:selfhost/engine/route_b/build_4b_artifact.sh  (0 match(es), expect_present=False)
  holds      ROUTE_B_4A_INVOKES_DART2BYTECODE
             worktree:selfhost/engine/route_b/build_4a_payload.sh:34  (1 match(es), expect_present=True)
  holds      ROUTE_B_4B_INVOKES_DART2BYTECODE
             worktree:selfhost/engine/route_b/build_4b_artifact.sh:96  (1 match(es), expect_present=True)

  resolved=8 refuted=0 unresolved=0

############ 1. THE MATRIX, EXTRACTED FROM EVIDENCE ############
  Every row is derived at run time from a named file and field or marker. A row
  whose evidence cannot be resolved reads NOT_ESTABLISHED and is never filled
  from a remembered conclusion. SEMANTIC-LINKER-1's assembler returned
  ABANDON_OR_REDESIGN on its first run because two rows pointed at the wrong
  transcript; the fix was the pointer, not the check. That happened again here:
  PRIVACY_CROSS_DOMAIN_REFUSAL first read NOT_ESTABLISHED because it was bound
  to G5/G6's marker convention, which G3 predates.

  24 rows extracted from 20 evidence files

  DECLARATION_ID_STABILITY           ESTABLISHED
  DECLARATION_ID_COLLISIONS          ESTABLISHED
  ABI_FINGERPRINT                    ESTABLISHED
  BODY_FINGERPRINT                   ESTABLISHED
  CLASSIFICATION_UNCHANGED           ESTABLISHED
  CLASSIFICATION_CHANGED_BODY        ESTABLISHED
  CLASSIFICATION_ABI_BREAK           ESTABLISHED
  PRIVACY_DOMAIN_DERIVATION          ESTABLISHED
  PRIVACY_CROSS_DOMAIN_REFUSAL       ESTABLISHED
  RETENTION_CONTRACT                 ESTABLISHED_WITH_LIMITS
  RETENTION_WITHHELD_FAILS_CLOSED    ESTABLISHED_NEGATIVE
  RETENTION_FAIL_OPEN_ROOT_CAUSE     RECONCILED_TO_CARRIED_CONSTRAINT
  DYNAMIC_INTERFACE_VALIDATION_MECHANISM OPTION_PRESENT_BUT_NOT_PASSED
  PATCHABILITY_SUBSET_PROPERTY       VACUOUS
  PATCHABILITY_DISTINCTION_REPRESENTABLE NOT_REPRESENTABLE
  DECIDABLE_EXCLUSIONS               NAMED_AND_FAIL_CLOSED
  OVER_CLAIM                         REFUSAL_ENFORCED
  MAP_RELEASE_BINDING                ESTABLISHED
  MAP_SCHEMA_VERSIONING              ESTABLISHED
  MAP_SIZE                           MEASURED_DETERMINISTIC
  GENERATION_TIME                    MEASURED_CURRENT_RUN_ONLY
  RETENTION_COST_AT_SCALE            MEASURED_DETERMINISTIC_PROJECTION_LOW
  REPRODUCIBILITY                    ESTABLISHED
  KNOWN_GAPS                         ENUMERATED

  NOT_ESTABLISHED: none
  KNOWN_GAPS entries: 14
  evidence files unreadable: none

############ 2. THE VERDICT, COMPUTED FROM THE MATRIX ############
  No verdict is seeded. Every predicate below is evaluated against row
  CATEGORIES and generated gap data, and all of them are published -- not only
  the one that selected -- so the decision surface is visible. If no predicate
  holds the verdict is NOT_ESTABLISHED: #58 forbids a generic FAIL, and
  inventing one of the five to fill the hole would be worse than saying the
  matrix does not classify.

  PREDICATES (all published, not only the selecting one)
    False  EVERY_REQUIRED_ROW_POSITIVE
           rows not in a positive category: ['RETENTION_CONTRACT', 'RETENTION_WITHHELD_FAILS_CLOSED', 'PATCHABILITY_SUBSET_PROPERTY', 'PATCHABILITY_DISTINCTION_REPRESENTABLE']
    False  PATCHABILITY_POSITIVELY_ESTABLISHED
           PATCHABILITY_SUBSET_PROPERTY=VACUOUS, admitted=0 of 36
    True   SUBSET_PROPERTY_VACUOUS
           the subset property holds only because the admitted set is empty; zero admitted is not positive patchability evidence
    True   UNSAFE_CLASS_MECHANICALLY_ACCOUNTED
           reader accounting 36 of 36 declarations; every declaration reaches a named state, so an empty admitted set is a decision rather than a coverage hole
    True   REFUSAL_PATH_ENFORCED
           OVER_CLAIM=REFUSAL_ENFORCED
    True   BLOCKING_PREREQUISITES_UNRESOLVED
           unresolved BLOCKING_FOR_PRODUCTION prerequisites: ['NON_STATIC_DISPATCH_PATCHABILITY', 'POST_HOC_REDIRECTABILITY_UNDECIDABLE', 'INLINING_EVIDENCE_MUST_COME_FROM_THE_SHIPPED_RELEASE']
    False  ANALYZER_DEFECT_ATTRIBUTED_BY_EVIDENCE
           gap entries attributing a shortfall to a tool or analyzer defect: none
    True   MODEL_ROWS_ALL_ESTABLISHED
           DECLARATION_ID_STABILITY=ESTABLISHED; DECLARATION_ID_COLLISIONS=ESTABLISHED; ABI_FINGERPRINT=ESTABLISHED; BODY_FINGERPRINT=ESTABLISHED; CLASSIFICATION_UNCHANGED=ESTABLISHED; CLASSIFICATION_CHANGED_BODY=ESTABLISHED; CLASSIFICATION_ABI_BREAK=ESTABLISHED
    True   RETENTION_WITHHELD_MEASURED_NOT_FAILING_CLOSED
           RETENTION_WITHHELD_FAILS_CLOSED=ESTABLISHED_NEGATIVE
    True   REPRESENTABILITY_EVIDENCE_SAYS_NOT_REPRESENTABLE
           PATCHABILITY_DISTINCTION_REPRESENTABLE=NOT_REPRESENTABLE
    True   DISTINCTION_NOT_REPRESENTABLE
           REPRESENTABILITY_EVIDENCE_SAYS_NOT_REPRESENTABLE alone. Neither the blocker count nor the size of the admitted set is an input: an unrelated prerequisite must not manufacture this verdict, and a non-empty admitted set must not suppress it.
    True   NAMED_DECIDABLE_EXCLUSION_PRESENT
           DECIDABLE_EXCLUSIONS=NAMED_AND_FAIL_CLOSED -- the class is named in the map contract AND its refusal is reached, not merely representable
    False  REPRESENTABILITY_DEFECT_ABSENT
           PATCHABILITY_DISTINCTION_REPRESENTABLE=NOT_REPRESENTABLE. A scope reduction cannot repair a representability defect: if the model cannot represent the distinction, a smaller input set does not make it able to.
    False  ADMITTED_SET_IS_SOUND
           non-empty, every declaration accounted for, and the over-claim refusal demonstrated -- a non-empty set that over-claims is not a reduced scope, it is an unsound one
    False  ADMITTED_SET_IS_PROPER_SUBSET
           admitted=0 of 36: strictly between empty and total, which is what "after EXCLUDING a class" means. A total admitted set excluded nothing and belongs to PROCEED.
    False  DECIDABLE_EXCLUSION_YIELDS_NONEMPTY
           NAMED_DECIDABLE_EXCLUSION_PRESENT and REPRESENTABILITY_DEFECT_ABSENT and ADMITTED_SET_IS_SOUND and ADMITTED_SET_IS_PROPER_SUBSET -- all four, per the FINAL ruling
    False  UNSAFE_CLASS_NOT_IDENTIFIABLE
           the negation of UNSAFE_CLASS_MECHANICALLY_ACCOUNTED
    False  ANALYZER_DEFECT_UNDER_VALID_MODEL
           ANALYZER_DEFECT_ATTRIBUTED_BY_EVIDENCE and MODEL_ROWS_ALL_ESTABLISHED -- both, so an attribution against an invalid model falls through rather than selecting or resetting
    False  ADMITTED_SET_IS_TOTAL
           admitted=0 of 36: the whole declared surface, so no feature class had to be excluded
    False  PROCEED_CONDITIONS_MET
           every required row positive, patchability positively established, refusals enforced, the admitted set TOTAL (nothing excluded -- a proper subset belongs to REDUCE_SCOPE), and no blocking prerequisite open

  LADDER, in #58 precedence order
    False  MODIFY_ANALYZER        via ANALYZER_DEFECT_UNDER_VALID_MODEL
    True   MODIFY_MAP_DESIGN      via DISTINCTION_NOT_REPRESENTABLE <== SELECTED
    False  REDUCE_SCOPE           via DECIDABLE_EXCLUSION_YIELDS_NONEMPTY
    False  ABANDON_OR_REDESIGN    via UNSAFE_CLASS_NOT_IDENTIFIABLE
    False  PROCEED                via PROCEED_CONDITIONS_MET

  VERDICT: MODIFY_MAP_DESIGN
  selected by: DISTINCTION_NOT_REPRESENTABLE

  NEXT LANE
    - The map cannot decide patchability by inference from a finished AOT, so patchability has to become a BUILD-TIME guarantee rather than a post-hoc classification. #59 (Mutable AOT Dart) is the accepted direction for that and may be named here; no implementation begins in #58.
    - AOT-ASSUMPTIONS-1 owns the optimizer-contract half and is still un-instrumented; it was deliberately not started here.
    - RETENTION_WITHHELD_FAILS_CLOSED is a measured NEGATIVE, and NOT a new finding: every class that fails open has cause DYNAMIC_INTERFACE_POLICY, while the one that fails closed (callable) has a different cause. SL1-G6C named the root cause MODULE_SIDE_DYNAMIC_INTERFACE_VALIDATION as BLOCKING_FOR_PRODUCTION and SL1-FINAL carried it into this lane. SL1's PREMISE does not hold against the frozen source: dart2bytecode registers --validate (dart2bytecode.dart:131) and assigns dynamicInterfaceSpecificationUri (:291), upstream and predating this fork. Its CONCLUSION does hold: validateDynamicModule is conditional on that URI (kernel_target.dart:1815) and Route B omits the flag (build_4a_payload.sh:34, build_4b_artifact.sh:96). So the next lane is a change to our own build scripts, not a compiler option -- and whether passing the flag makes the negatives fail closed is UNTESTED, being that lane's first experiment. Independent of patchability; must close before any map is relied on.
    - Generation-time figures are current-run observations. Any schedule or budget derived from them needs a repeated measurement at the scale being shipped, not this sample.

############ 3. PROVENANCE ############
  The input set comes from the extractor's access log -- the files it actually
  opened, digested at read time -- cross-checked against the registry. A file a
  row names but the extractor never opened, or vice versa, is a finding. This
  manifest does not digest itself, RESULT.md, the matrix or the verdict: they
  are outputs of this assembly. G5 once produced a manifest that included its
  own output, and a second run never reproduced the first.

  consumed evidence files : 20
  declared by rows        : 20
  sets equal              : True
  producing scripts       : 60
  assembler components    : 9
  findings                : none
  inputs without a digest : none

############ 4. RESULT.md, GENERATED FROM THOSE ARTIFACTS ############
  RESULT.md generated: 167 lines, 24 rows, verdict MODIFY_MAP_DESIGN

############ 5. SENSITIVITY CONTROLS ############
  The published verdict is only worth something if a DIFFERENT verdict was
  reachable. Family B drives the computation to each of the five allowed
  verdicts in turn, and to NOT_ESTABLISHED, from a described state of the
  evidence. Family A removes and corrupts every row's evidence in turn.
  Family C proves the arms discriminate, by showing a weakened assembler that
  hard-codes the baseline satisfies exactly one family-B arm -- the baseline's
  own -- and no other. Family D runs this orchestrator against damaged
  evidence and checks what RESULT.md then says.

SM1-FINAL -- sensitivity controls

  baseline verdict: MODIFY_MAP_DESIGN
  baseline NOT_ESTABLISHED rows: none

  A. PER-ROW EVIDENCE SENSITIVITY (derived: every row x every file it names)
     Removing or corrupting a row's evidence must make THAT row read
     NOT_ESTABLISHED. A row that survived losing its own evidence would
     be answering from somewhere other than the evidence.
     35 (row, file) pairs x 2 mutations = 70 arms
     arms=70 failed=0

  B. VERDICT REACHABILITY -- every allowed verdict must be reachable
     If only one verdict could ever come out, publishing it proves
     nothing about the evidence.
     pass  PROCEED                <- the WHOLE declared surface admitted, retention failing closed, no blocking prerequisite, and the distinction representable -- nothing was excluded, so this is not a reduced scope
     pass  MODIFY_ANALYZER        <- a shortfall ATTRIBUTED to an analyzer defect while the model rows all hold
     pass  MODIFY_MAP_DESIGN      <- the evidence as it stands: empty admitted set, full accounting, blocking prerequisites open
     pass  REDUCE_SCOPE           <- the representability defect repaired, a real named decidable exclusion still in force, and a PROPER SUBSET admitted while retention still does not fail closed
     pass  ABANDON_OR_REDESIGN    <- the refusal accounting no longer covers every declaration, so the unsafe class is not mechanically identifiable -- with representability repaired, because otherwise the higher-priority map-design rung correctly wins and this label is unreachable for the right reason
     pass  NOT_ESTABLISHED        <- no predicate holds -- the distinction is representable but the admitted set is still empty, so neither map-design nor reduce-scope nor proceed applies; the ladder must not default
     arms=6 failed=0

  B2. PRECEDENCE OVERLAP
     Reaching every label once proves the labels exist. It does NOT
     prove precedence. These arms construct states where TWO rungs
     could fire and require the earlier one to win, and states that
     distinguish a representability blocker from an unrelated one.
     pass  proceed-state + valid analyzer defect -> MODIFY_ANALYZER
           PROCEED conditions hold AND a valid-model analyzer defect is attributed. With PROCEED first -- as it was -- this shipped over a known bug.
     pass  analyzer attribution + invalid model -> falls through to MODIFY_MAP_DESIGN
           An attribution against an INVALID model must not select MODIFY_ANALYZER, and must not reset the verdict either -- the ladder continues to the next rung.
     pass  representability blocker alone -> map-design predicate TRUE
           Every production prerequisite resolved, representability evidence kept. The predicate must still hold: the blocker count is not an input to it.
     pass  representability resolved, unrelated blocker remains -> map-design predicate FALSE
           Blocking prerequisites still open, representability evidence flipped. An unrelated prerequisite must NOT manufacture a map-design verdict -- this is the defect that made the old predicate too broad.
     pass  NOT_REPRESENTABLE + admitted>0 -> still MODIFY_MAP_DESIGN
           A synthetic non-empty admitted set cannot repair a representability defect. The old predicate also required the subset to stay VACUOUS, so this state DISARMED the higher-priority rung -- an invalid decision surface.
     pass  retention failure alone cannot manufacture REDUCE_SCOPE
           The baseline already has a measured retention negative and a vacuous patchability result. Neither is an EXCLUSION, and the old predicate accepted either as one.
     pass  a real named decidable exclusion CAN produce REDUCE_SCOPE
           The exclusion is the bank's own -- G2's ROADMAP P2 ABI boundary, the unsupported-body refusal and G3's exercised privacy refusals. Nothing was invented to make this label reachable; the counterfactual is the repaired representability and the proper-subset admitted set.
     pass  fully green + TOTAL admitted + nothing excluded -> PROCEED
           The whole declared surface is admitted, so no class was excluded and REDUCE_SCOPE must not fire ahead of PROCEED. Proper-subset versus total is what separates the two rungs.
     arms=8 failed=0

  C. WEAKENED ASSEMBLER -- one that hard-codes the baseline verdict
     must FAIL family B. Without this, family B proves the assembler
     reacts to input, not that it reacts correctly.
     overlap arms the weakened assembler still satisfies: 0
     pass  it satisfies no overlap arm
     family-B arms the weakened assembler still satisfies: 1
     pass  it satisfies only MODIFY_MAP_DESIGN, which IS the baseline

  D. STALE OUTPUTS -- a previous RESULT.md and manifest must not read as
     current output after an assembly that failed. This runs the REAL
     orchestrator against corrupted evidence. The first version of this
     control deleted the stale files ITSELF and then asserted they were
     gone, which could not fail -- it was checking its own cleanup, not
     run_final.sh's contract.
     pass  degraded: orchestrator exits non-zero
     pass  degraded: the stale RESULT.md did not survive
     pass  degraded: RESULT.md leads with an INCOMPLETE banner
     pass  degraded: the verdict is marked PROVISIONAL
     pass  degraded: RESULT.md names the unresolved row
     pass  degraded: the stale manifest did not survive
     pass  crashed: orchestrator exits non-zero
     pass  crashed: the stale RESULT.md did not survive
     pass  crashed: RESULT.md says ASSEMBLY FAILED
     pass  crashed: RESULT.md does not claim a verdict

  TOTAL arms=108 failed=0
SM1_FINAL_SENSITIVITY: EVERY_CONTROL_DISCRIMINATES

ASSERTIONS (32 checked: 32 pass, 0 fail, 0 note)
  pass  the matrix extracted
  pass  every declared row is present
  pass  no row is NOT_ESTABLISHED
  pass  no positive row is also listed as a known gap
  pass  the positive set came from the registry
  pass  every positive category is in the declared vocabulary
  pass  the source record was produced
  pass  the source record is about the frozen tree
  pass  the routing mechanism is derived, not asserted
  pass  every evidence file was readable
  pass  the verdict computed
  pass  the verdict is one of the five allowed
  pass  the verdict was selected by a named predicate
  pass  exactly one ladder rung selected
  pass  the ladder follows #58's precedence order
  pass  the selected rung is the FIRST true one
  pass  each rung predicate is the conjunction it claims
  pass  PROCEED and REDUCE_SCOPE are mutually exclusive by construction
  pass  every predicate is published
  pass  the provenance manifest built
  pass  every consumed input carries a digest
  pass  the input set equals the declared set
  pass  the manifest reports no findings
  pass  the manifest does not digest its own outputs
  pass  RESULT.md generated
  pass  RESULT.md is machine-generated, not authored
  pass  RESULT.md carries the computed verdict
  pass  RESULT.md enumerates the non-proven claims
  pass  the sensitivity controls discriminate
  pass  this run is not a control-free nested invocation
  pass  RESULT.md is a report, not a failure notice
  pass  the published matrix is complete, so no PROVISIONAL verdict

SM1_FINAL: MODIFY_MAP_DESIGN
