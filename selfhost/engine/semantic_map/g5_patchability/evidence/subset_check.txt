SM1-G5 Phase D -- predicted patchable SUBSET-OF demonstrated patchable
generated 2026-09-09T01:41:38Z by run_subset.sh

SUBJECT (the canonical release, taken not rebuilt)
  7010c68d7ba14ac9a666a012436fb6b131f8a066c43e14a8375974c85992ea34  app_release.aot
  b9b29d23f37e5e11a5ab181f7f9674c5a482f52b5bcd0483272e6f57c0f90550  evidence/g1_projection.json
  8e97a2d235486e855be0dfebd5d532744354c1e1ed4fede9408fe413ca48501a  evidence/inlining_state.json

############ 1. RELEASE CONTRACT, FROM THE RELEASE ITSELF ############
  release declarations=167 ambiguous=0 -> /var/folders/bn/jq675y6x5m1d1x83vyh99f5c0000gn/T//sm1_g8/probe/release_contract.json
  capability        PROVEN
  evidence          note: schema_version=1;patchable_static_calls=true
  release_aot_sha256 7010c68d7ba14ac9a666a012436fb6b131f8a066c43e14a8375974c85992ea34
  contract rows     167

############ 2. THE EVIDENCE CHAIN, REGENERATED ############
  CORRECTION. An earlier version of this transcript said no checked-in script
  produces the G1 projection, privacy rows, retention rows or body-reference
  rows for this corpus. That was WRONG. All four producers are checked in and
  run; they had simply never been pointed at this library. They are regenerated
  here, so the prediction rests on produced evidence rather than on empty
  inputs standing in for missing ones.

wrote g1_regen.json  rows=36
  rows=36 private=12 write-collapsed=0 -> g3_rows.json
  retention rows=36 -> g4_rows.json
  bodies=31 private-type-refs=4 private-writes=0 traversal-refused=0 -> refs_rows.json

  the regenerated G1 projection must equal the banked copy BYTE FOR BYTE:
    equal  b9b29d23f37e5e11a5ab181f7f9674c5a482f52b5bcd0483272e6f57c0f90550

############ 3. PREDICTION -- RELEASE EVIDENCE ONLY ############
  predictions=36 patchable=0 refused=36 -> /var/folders/bn/jq675y6x5m1d1x83vyh99f5c0000gn/T//sm1_g8/probe/predictions.json
  declarations        36
  predicted patchable 0
  refusal histogram:
     20  RETENTION_UNPROVEN
     18  ABI_SHAPE_UNSUPPORTED
     18  CALL_SITE_SHAPE_UNPROVEN
     18  FRONTEND_MATERIALIZATION_UNPROVEN
      6  BODY_ENCODING_REFUSED
      4  PRIVATE_TYPE_REFERENCE
      3  PRIVATE_REFERENCE_UNRESOLVED
      2  INLINED_BODY
      2  MISSING_CAN_BE_OVERRIDDEN
      2  NON_STATIC_DISPATCH_UNPROVEN
      1  INLINING_STATE_UNKNOWN

############ 4. DEMONSTRATION -- THE SHIPPING PATH ############
  alpha            demonstrated     moved=['alpha'] stale=[] (invoke=PATCHED-a)
  Base.work        NOT demonstrated moved=['direct'] stale=['virtual', 'inlined'] (invoke=PATCHED-w)
  smallTarget      NOT demonstrated moved=[] stale=['small', 'callsSmall'] (invoke=141)
  tearOffTarget    demonstrated     moved=['tearOff'] stale=[] (invoke=171)

  A declaration counts demonstrated only when EVERY observed ordinary call site
  moved. Container parsing, attach success, a direct C++ invoke, predictor
  agreement and the absence of a bad machine-code shape are recorded for
  context and ignored by the scorer. The call-site map is parsed out of the
  probe's own _state(), and a printed field that no target maps and nothing
  excludes is a hard failure -- an unmapped call site is how a stale site would
  go unobserved.

############ 4b. THE SAME SET, DERIVED WITHOUT A CALL-SITE MAP ############
  The map above is parsed from the probe's _state(). A real application has no
  _state(), so that method does not generalise. This derives the same set from a
  SINGLE-TARGET DIFF instead: each container carries one target, so any change
  in the program's observable state is attributable to it.

      moved = fields whose value changed
      stale = fields still showing a value the patch moved elsewhere
      demonstrated = moved non-empty AND stale empty

  No field is named in advance, and the scorer RECOMPUTES the derivation from
  the recorded before/after state rather than trusting the row -- so deleting a
  stale site, with or without lowering the declared count, is refused.

  If an unrelated declaration coincidentally shares a moved-from value, its
  field is misread as a stale site of this target. That OVER-reports staleness,
  which withdraws a demonstration rather than manufacturing one. Only call
  sites exercised during the run are observed; that limit is reported, not
  implied.

  alpha            demonstrated     moved=['alpha'] stale=[] observed=9
  work             NOT demonstrated moved=['direct'] stale=['inlined', 'virtual'] observed=9
  smallTarget      NOT demonstrated moved=[] stale=[] observed=9
  tearOffTarget    demonstrated     moved=['tearOff'] stale=[] observed=9

  the two methods must agree, declaration by declaration:
  0d9096c2ad9a4f44  mapped=False derived=False agree
  b6a0c33f10c0cf40  mapped=True  derived=True  agree
  bff08abe1a4b7690  mapped=False derived=False agree
  dbcb9971475a4d21  mapped=True  derived=True  agree
  ALL AGREE

############ 5. THE SUBSET CHECK ############
  declarations          36
  predicted patchable   0
  refused               36
  coverage (diagnostic) 0/36 = 0.0%
  prediction_set          EMPTY
  behavioral_demonstration SUPPLIED
SM1_G5_SUBSET: SUBSET_HOLDS_VACUOUSLY

############ 6. THE SCORER MUST BE ABLE TO FAIL ############
  The subset property is trivially satisfied while nothing is predicted
  patchable, so the verdict alone carries no information. What carries
  information is that the scorer fails on an injected over-claim and stays PASS
  under merely reduced coverage.

arm                                                        result detail
--------------------------------------------------------------------------------------------------------
BASELINE the banked pair                                   pass   SUBSET_HOLDS_VACUOUSLY
over-claim: a declaration with a stale ordinary call site  pass   FAIL_OPEN
over-claim: a declaration whose every call site is stale   pass   FAIL_OPEN
over-claim: a declaration with no demonstration row at all pass   FAIL_OPEN
a demonstrated declaration may be predicted                pass   SUBSET_HOLDS
reduced coverage is safe (nothing predicted)               pass   SUBSET_HOLDS_VACUOUSLY
attach and direct invoke alone do not demonstrate          pass   FAIL_OPEN
under-observation: stale sites deleted from the demonstration pass   FAIL_OPEN
under-observation: stale sites deleted AND the count lowered pass   FAIL_OPEN
the recorded call sites no longer account for the observed state pass   FAIL_OPEN
the demonstration declares itself predictor-derived        pass   FAIL_OPEN
the demonstration carries no completeness metadata         pass   FAIL_OPEN
--------------------------------------------------------------------------------------------------------
arms=12 passed=12 failed=0
SM1_G5_SUBSET_FALSIFICATION: SCORER_IS_SENSITIVE
exit=0  (asserted: 0)

############ 7. CORPORA STILL OUTSTANDING ############
  Phase D requires the adversarial corpus, Wonderous and LocalSend. Only the
  adversarial corpus is run here.

  The producer chain is NOT the blocker -- section 2 corrects that. Nor is the
  call-site map, any more: section 4b derives the same verdicts without one, so
  the method now generalises to a corpus that has no _state().

  What each app corpus still needs is its own release built through this exact
  toolchain -- a kernel compiled WITH --dynamic-interface, an AOT from the
  instrumented gen_snapshot, a generated dynamic interface, and patch
  containers for chosen targets -- plus some observable state to diff, and an
  exercise that actually reaches the targets. The observable state is the open
  design question for a GUI app: this probe prints a line, and Wonderous does
  not.

  Running them is not attempted and is NOT reported as passing.

ASSERTIONS (14 checked)
  pass  no producer in this run failed
  pass  the release contract binds to the canonical subject
  pass  route 2 evidence is bound to the same artifact
  pass  the subset property holds (vacuously, nothing admitted)
  pass  the G1 projection regenerates byte-identically
  pass  real privacy evidence reached the predictor
  pass  real retention evidence reached the predictor
  pass  body-reference evidence was produced, not stubbed
  pass  route 2 actually reached the predictor
  pass  the scorer fails on an injected over-claim and passes otherwise
  pass  the falsification records SCORER_IS_SENSITIVE
  pass  the map-free derivation agrees with the mapped one
  pass  the subset holds on the derived demonstration too
  pass  the scorer is sensitive on the derived demonstration too

SM1_G5_SUBSET_BANK: ADVERSARIAL_CORPUS_ONLY
