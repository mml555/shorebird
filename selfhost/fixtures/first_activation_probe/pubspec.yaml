name: first_activation_probe
description: "Arm C fixture: boot-time patch signature verification."
publish_to: 'none'
version: 1.3.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - shorebird.yaml
