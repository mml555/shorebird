# This file is used to configure the Shorebird updater used by your app.
# Learn more at https://docs.shorebird.dev
# This file does not contain any sensitive information and should be checked into version control.

# Your app_id is the unique identifier assigned to your app.
# It is used to identify your app when requesting patches from Shorebird's servers.
# It is not a secret and can be shared publicly.
app_id: c8572c59-20d3-aabc-f400-82f3736e395b

# auto_update controls if Shorebird should automatically update in the background on launch.
# If auto_update: false, you will need to use package:shorebird_code_push to trigger updates.
# https://pub.dev/packages/shorebird_code_push
# Uncomment the following line to disable automatic updates.
# auto_update: false

# THE SELF-HOSTED CONTROL PLANE. Without this the on-device updater talks to
# api.shorebird.dev and the specimen measures nothing. Derived at run time by
# selfhost/scripts/prepare_ios_endpoint.sh -- never a constant, because both the
# Mac's address and the container's advertised URL drift independently.
base_url: http://10.0.0.7:18080
