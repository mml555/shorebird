# G6/G7/G8/G10.2 — the no-hardware lanes

> At the end, `G6`'s server rows are backed by named non-`stable` tracks in our own test suite rather than by a generic code path, `G7` is a written decision in `selfhost/SIGNING.md` instead of an open question, `G8` has a fixture that drives updates from Dart, and `G10.2` has a harness proving the real release/patch path completes against *our* control plane with no TTY -- none of which needed a device, a mint, or the canonical fixture.

| field | value |
|---|---|
| status | **PARTIAL** -- important cases work server-side (`G6`, and `G10.2`'s prompt machinery is fully built, and `G7`'s CLI test half is **already written**); the surface is not covered, and `G7` is a **KNOWN GAP** the doc mis-describes |
| owns | `R10` `packages/code_push_server` source + tests. Also edits `packages/shorebird_cli`, `packages/code_push_runtime`'s consumers, and `selfhost/scripts/` -- **none of those is an R-id**, so say so out loud rather than assuming a claim covers you |
| excludes | nothing on the resource table -- **but two files it edits are other lanes' unrun harnesses.** `selfhost/scripts/airgap_acceptance.sh` is `G13`'s harness (`PARITY.md:2452`) and carries a **BUILT, NOT YET RUN** iOS code-patch stage (`:2488`); `selfhost/scripts/accept_android_default.sh` is `G4.2`'s Android-half harness and the source of `R12`'s Linux constraint (`:2710`), device gate still owed (`:3210-3215`). Neither is an R-id, which is exactly why the collision is invisible. Otherwise genuinely concurrent with the `R1`/`R3`/`R6`/`R11` mint-and-device lane (commit `ba4e1c02` minted cell `4df8f9b6` and touched none of these files). The other real conflict is **another `G6` worker in `api_test.dart`** |
| blocked by | nothing for A/B/C-host/D. `G6`'s **device** row is blocked by `G8` step 14 or by `shorebird preview --track` (queue item 10, `PARITY.md:3216-3217`) -- and is explicitly **out of scope here** |
| unblocks | `G6`'s device row (via the `G8` fixture); `G7` device rejection proof (needs `R1`/`R2`, not this order); honest CI for every later goal |
| device needed | **none.** Any step that wants a phone belongs to a different order |
| mint needed | **no.** Nothing here is inside the compiler cell |
| est. shape | four host-day-or-less units, sequenced A→D. **A is smaller than it looks** -- its CLI test already exists, so A is a doc plus a retraction. **D is larger than it looks** -- two harness arms, a new script, and a flag audit -- and it is the one with a real chance of surprising you |

### The four, each with its own status line

| lane | current PARITY status | rows | owns | scope here |
|---|---|---|---|---|
| **A · `G7 signing`** | **KNOWN GAP** (§7, `PARITY.md:2214-2263`) -- the default verifies nothing | 4 gap/not-built rows | none (docs only; the CLI test already exists) | the **decision**, `SIGNING.md`, and a correction to §7's stated mechanism |
| **B · `G6 tracks`** server half | **BUILT** for promote/withdraw (`PARITY.md:2185-2186`); **5** rows **NOT VALIDATED** | `:2181`-`:2184`, `:2187` | `R10` | named-track tests + the "move"→"add" fix in the CLI. **Not** the device row |
| **C · `G8 manual-api`** | **NOT VALIDATED** ×5 (`:2270`-`:2274`) + one **KNOWN GAP** (`:2275`); §8 spans `:2265-2288` | `:2270`-`:2273` | its own new fixture | host + Android device-ready fixture; iOS row stays owed |
| **D · `G10.2 noninteractive`** | **NOT VALIDATED** ×3 + **KNOWN ISSUE** (`PARITY.md:2361-2364`) | `:2361`, `:2362`, `:2363`, `:2364` | `packages/shorebird_cli` | prove it against **our** control plane; correct one stale script comment; leave the other alone |

**Provenance.** Grounded against the tree and then adversarially verified — a second pass
re-read every cited path, line anchor and command. That verification ran at `ba4e1c02`, **before**
`c0619d13` landed, so this file has not been re-checked against the four prerequisite blocks that
commit recorded. `PARITY.md:NNNN` anchors move whenever that file is edited: if one looks wrong,
re-locate by grepping the quoted heading. Schema and house rules: [`README.md`](README.md).

## Why this is the piece it is

These four share one property that nothing else on the queue shares: **no contended hardware.** `R10` is "standalone package, own lockfile. Cheap to work on concurrently with anything" (`PARITY.md:2708`), and the other three lanes touch only CLI source, docs, and a fixture that does not exist yet. That means this whole roof can be handed to a fresh agent while the expensive mint/device lane runs, which is exactly what §16's four-lane ceiling contemplates (`PARITY.md:2760-2770`, ceiling stated `:2780-2782`; the Server lane is row `:2768`).

It deliberately does **NOT** include: `G6`'s device row (no channel reaches the device -- see step 10's finding, and it needs `R1`/`R2`); `G7`'s on-device tamper-rejection proof (`R1`/`R2`); `G8`'s iOS Route B row (`R1`, and its restart-required half rides `G15`'s once-per-process guard, `PARITY.md:2275` linked to `G15` at `:2337-2340`); progressive-rollout *device* behavior. It also does not include `G10.1 stale-ipa`, already **BUILT** at `c57c6537` (`PARITY.md:2365`).

## Preconditions -- check these before claiming anything

1. **Read the tree first, and expect it to move.** `git log --oneline -3 && git status --short`. As of 2026-08-13 HEAD is `ba4e1c02` ("mint the batch cell 4df8f9b6") and the tree is **clean** -- the staged set an earlier pass observed (`selfhost/PARITY.md`, `selfhost/cdn/experimental_hashes.map`, `selfhost/engine/route_b/probes/assert_mint_ready.sh`) is committed. A live worker has been in this tree within the hour. Expect: clean tree, HEAD at or ahead of `ba4e1c02`.
2. **Check the index before you ever type `git commit`.** `git diff --cached --name-only` must print nothing that is yours-plus-someone-else's. If it prints another worker's files, do **not** commit -- `git commit -m …` with no pathspec sweeps the whole index. Use the pathspec form (step 20's commit block).
3. **`git worktree list`.** Expect **four** entries: `/Users/mendell/shorebird` on `feat/engine-improvements`, plus `shorebird-compat-study{,2,3}` at detached `aa36f92d`/`8f5d7d4b`/`2da51f65`. §17's governing sentence "`git worktree list` returns exactly one entry" (`PARITY.md:2793-2795`) is **stale** -- but the three extras are detached study checkouts, not landing places. Per §17 rule 3 (`:2806-2808`), code work belongs in its own worktree: `git worktree add /Volumes/build/dartverify/wt-nohw feat/engine-improvements` is *not* right (shared branch); prefer `git worktree add -b lane/no-hardware ../shorebird-nohw feat/engine-improvements` and cherry-pick back, or accept the shared tree and stage explicit paths only.
4. **Claim `R10`.** `sed -n '2858p' selfhost/PARITY.md` -> today `| `R10` server source | — | — | — | **free** — the `G6` lane |`. Write your row **in the same commit as the work** (§17 Claims, `PARITY.md:2845-2846`). A write claim must carry **TREE HEALTH: GREEN/RED** (`PARITY.md:2876-2896`, definitions at `:2893-2896`) -- for `R10` that means the three commands CI runs, in CI's order: `dart format --output=none --set-exit-if-changed .` (`code_push_server.yaml:44`), `dart analyze --fatal-infos --fatal-warnings` (`:47`), `dart test -x integration` (`:58`).
5. **Note the stale rows you did not cause.** `sed -n '2852p;2859p' selfhost/PARITY.md` still say `R3`/`R11` are free and publishing/serving `ee001fd7`, but `ba4e1c02` minted `4df8f9b6`. Do **not** silently fix another worker's claim row; record it in your commit body as an observation. Stale rows are worse than no rows (`PARITY.md:2845`).
6. **Server suite is green before you touch it.** `cd packages/code_push_server && dart test -x integration`. This is the exact CI command (`.github/workflows/code_push_server.yaml:23` `working-directory`, `:58` `run: dart test -x integration`). The `integration` tag is opt-in per `packages/code_push_server/dart_test.yaml`. The package is **not** a workspace member -- root `pubspec.yaml:5-18` lists 13 packages and omits it (**and omits `packages/code_push_runtime`, which lane C reasons about**) -- so it has its own `pubspec.lock` and must be run from its own directory.
7. **CLI suite is green.** `dart test packages/shorebird_cli`. Workspace-wide: `cd packages && very_good test -r`.
8. **`selfhost/SIGNING.md` does not exist** (`test -f selfhost/SIGNING.md` -> false). Writing it is step 3.
9. **`selfhost/plans/` does not exist yet.** Creating it is part of landing this file.
10. **Nothing lints `selfhost/scripts/`.** `code_push_server.yaml:50-54` runs `git ls-files -z '*.sh' | xargs -0 -r shellcheck -x --source-path=SCRIPTDIR --severity=warning`, but under `working-directory: packages/code_push_server` (`:23`), so it only sees that package's scripts, and no other workflow runs shellcheck at all. Run that exact command by hand on any script you add (step 22).

## Steps

### A · `G7` -- the decision, before any work

1. **Make the decision explicit, and write the options down before choosing.** The security boundary is the device and nothing on the server participates. Three options:
   * **(i) Ship as-is, documented.** Default = no verification; `SIGNING.md` says so in the first paragraph. Cost: zero code. Risk: a self-hoster reasonably assumes signing is on.
   * **(ii) Make signing opt-in but *loud*.** Keep the default, add a `shorebird doctor` / release-time warning when no `--public-key-path` was supplied. A precedent already exists in the opposite direction: `packages/shorebird_cli/lib/src/commands/release/release_command.dart:423` tests `shorebirdYaml?.patchVerification != null && !hasPublicKey` and warns at `:424-431` when `patch_verification` is set *without* a public key source. Cost: one warning + tests.
   * **(iii) Fail closed.** Refuse to publish a release without a public key. Cost: breaks every existing self-host flow including our own acceptance scripts. Almost certainly wrong for a fork whose Android and iOS legs are already PROVEN unsigned.
   Recommend **(ii)**; record the choice and the rejected options in `SIGNING.md`. **This is a design call and needs no hardware** -- the classification rule (`PARITY.md:3268-3277`) is explicit that a gap dispatches "we should decide whether we ship that", not a device booking.
2. **Correct §7's stated mechanism -- the headline is right, the reason given is wrong.** Verified 2026-08-13 at HEAD `ba4e1c02`:
   * `vendor/updater/library/src/yaml.rs:3-12` -- the enum's `#[default]` is **`Strict`** (`:7-8`), not `InstallOnly`. So `PARITY.md:2231`'s "the default `patch_verification: install_only`" is **false about which mode is the default**.
   * What actually makes it verify nothing is the *second* gate. `vendor/updater/library/src/cache/lifecycle.rs:796` `if mode == PatchVerificationMode::Strict {`, then `:797` `if let Some(public_key) = public_key`, else `:802` `shorebird_info!("No public key configured; skipping signature verification");`. No public key is configured unless `--public-key-path` is passed at release time, which sets `SHOREBIRD_PUBLIC_KEY` in the build env, which `compileShorebirdYaml` copies to `patch_public_key` (`vendor/flutter/packages/flutter_tools/lib/src/shorebird/shorebird_yaml.dart:71-74`).
   * `install_only` verifies **nowhere in production**, and you must show *both* call sites or a reader will find the second one and disbelieve you. There are exactly two `check_signature` calls outside `signing.rs`: `lifecycle.rs:800`, inside `validate_installed_patch` (`:771`), reached only from `validate_next_boot_patch` (`:686`, declared `:678`, exposed via `updater_state.rs:330-332` → `updater.rs:1172` → `c_api/engine.rs:158`) -- i.e. **boot**, in `Strict` only; and `updater_state.rs:363`, inside `install_patch`, which is **`#[cfg(test)]`-gated at `:347`** and documented at `:340-346` as having no production caller. The production install path is `lifecycle::record_install_complete` (`:897-922`), which copies the stored signature into `PatchState::Installed` and verifies **nothing**. So the "install" in `installOnly` is a lie in *both* directions -- and `packages/shorebird_cli/lib/src/config/shorebird_yaml.dart:11-13` documents it as "Verify the patch signature and hash before installing, but not when loading from cache", which no production code does.
   * Net: **the headline "THE DEFAULT VERIFIES NOTHING" survives; the mechanism sentence at `:2231` must be rewritten.** A correction needs evidence as much as the original claim (`PARITY.md:3288-3289`, and verify at the cited location `:3296-3299`), so cite `yaml.rs:7-8`, `lifecycle.rs:796-803`, `lifecycle.rs:897-922` and `updater_state.rs:347` where the claim sits, and per `PARITY.md:3305-3307` say the earlier text was wrong rather than editing it away.
3. **Write `selfhost/SIGNING.md`.** Sections: what verifies (nothing, by default, and why); the exact two gates with the file:line above, including the `#[cfg(test)]` install-time site so nobody re-discovers it as a contradiction; the fixed algorithm -- `packages/shorebird_cli/lib/src/code_signer.dart:45` `Signer('SHA-256/RSA')`, which is what makes the "KMS-backed" row aspirational; the flag matrix `--public-key-path` / `--private-key-path` / `--public-key-cmd` / `--sign-cmd` (`packages/shorebird_cli/lib/src/common_arguments.dart:107,116,125,135`) and its validation rules (`packages/shorebird_cli/lib/src/extensions/arg_results.dart:129-141`, enforced by `assertAbsentOrValidKeyPairOrCommands()` at `:142`); a **rotation procedure** (currently **NOT BUILT**, `PARITY.md:2235-2236` -- write the manual procedure: new keypair, re-release, old patches unverifiable; do not invent a mechanism); and the `SBRBPTCH` note that signing takes a hash *string* (`vendor/updater/library/src/cache/signing.rs:37`) so container shape is structurally irrelevant.
   *Check:* a reader who has never seen this repo can answer "is my patch signed?" from the first screen.
4. **Do not write the CLI test -- it already exists. Run it, record it, and name the seam it does not cover.** §7 says the code half is "one CLI test plus a `SIGNING.md`" (`PARITY.md:2260-2261`). That test is already in the tree: `packages/shorebird_cli/test/src/code_signer_test.dart` covers `sign` with a PKCS#8 key (`:47`) and a PKCS#1 key (`:72`), asserts the two agree (`:97`), covers `base64PublicKey` (`:113`) and `base64PublicKeyFromPem` (`:137`), and does the full round trip -- `codeSigner.sign` then `codeSigner.verify` true (`:150`), false for a bad signature (`:167`), false for a wrong message (`:180`) -- plus `runPublicKeyCmd` (`:213`) and `signWithCmd` (`:310`). So §7's code half is **already BUILT**; claiming it as your work would be crediting an existing test to this order. Two honest additions instead: (a) note that the openssl group is `onPlatform` `Skip`ped on Windows, so it is not a cross-platform gate; (b) the genuinely untested seam -- `CodeSigner.verify` takes a **PEM** (`code_signer.dart:151`) while the updater's `ring` verifier consumes **base64 DER** produced by `base64PublicKey` (`:56-61`); nothing anywhere asserts that a Dart-produced signature verifies under the Rust side's key encoding. That, plus the still-NOT VALIDATED `--sign-cmd` row (`PARITY.md:2237`), is what a new test would be for. *Check:* `dart test packages/shorebird_cli` green, and record which tests already existed.
5. **Do not look for RSA in the server.** `packages/code_push_server/lib/src/signing.dart` is **URL** signing -- `UrlSigner`, HMAC-SHA256 over `"<token>.<exp>"` for time-bounded download URLs. The server's whole patch-signing role is to store `hash_signature` verbatim (`repository.dart:1370,1378,1388,1439`; read at `api.dart:1513`) and echo it back (`api.dart:2063`, `:2128`). Confirm by reading, then say so in `SIGNING.md`. §7 already records that the package carries no RSA library (`PARITY.md:2257-2259`).

### B · `G6` -- the server half, named rather than generic

6. **Establish what is actually already covered, because §6 retracted a claim here twice.** `grep -rn "beta" packages/code_push_server/lib packages/code_push_server/test` returns nothing today, and `staging` matches only `ArtifactStore` upload staging. **That is exactly the negative grep §6 retracted** (`PARITY.md:2201-2204`) -- do not repeat it. What *is* covered: `packages/code_push_server/test/api_test.dart:2830-2833` carries the comment "Tracks are channels: `shorebird patches set-track --track=<name>` resolves the channel and promotes onto it", and the group `'independent live patches per track'` (`:2834`) creates a channel literally named `internal` (`:2898` `await channel('internal')`) and asserts per-track `check()` results across two promotions (`:2904-2916`). So a non-`stable` channel **is** already exercised. The generic code path: `api.dart:1550` `_createChannel` get-or-create by name, `repository.dart:1131` `createChannel`, `api.dart:1675` `_getChannels`.
7. **Add the named rows §6 lists as NOT VALIDATED, as tests.** In `packages/code_push_server/test/api_test.dart`, extend the existing group rather than starting a new one: a `beta` channel and a `staging` channel alongside `stable`; publish a patch to each; assert `POST /api/v1/patches/check` with `'channel': 'beta'` returns beta's patch and *not* stable's. Then assert promote's supersession stays inside the channel -- the anchor is `packages/code_push_server/lib/src/repository.dart:1205` `'WHERE channel_id = @c AND status = @a AND patch_id <> @p '`, which is what settles the verb as **ADD**. Withdraw is likewise channel-scoped (`repository.dart:1336` declaration, `:1342` `'WHERE channel_id = @c AND patch_id = @p AND status = @a'`). *Check:* `cd packages/code_push_server && dart test -x integration`.
8. **Progressive rollout: server BUILT, and the CLI cannot reach it -- classify before testing.** `packages/code_push_server/lib/src/rollout.dart:19` `eligibleForRollout` is deterministic-bucket and fails closed with no `clientId`; `test/rollout_test.dart` carries 10 tests; the percentage is settable via `_promotePatch`'s optional body field (`api.dart:1684` declaration, `:1688` `_optIntField(body, 'rollout') ?? 100`, `:1689-1690` range check, `:1697` `repo.promote(..., rollout: rollout)`) and the admin route `POST /admin/apps/{appId}/patches/{patchId}/rollout?channel=&percent=` (`api.dart:2497` route comment, handler branch `:2533`, audit label `:2546`). But `rollout` appears in **none** of `packages/shorebird_cli/lib/src`, `packages/shorebird_code_push_client/lib/src`, `packages/shorebird_code_push_protocol/lib`, or `PromotePatchRequest` (whose fields are `patchId` `:37` and `channelId` `:40` only). **Source determines this: it is a KNOWN GAP in the client surface, not an unvalidated row** (`PARITY.md:3268-3277`). Re-run the greps yourself; then either add the field or record the gap -- do not book a run.
9. **Fix the "move" language §6 asks for -- in the CLI only; PARITY §6 is already fixed.** `PARITY.md:2206-2210` says fix it "wherever it says 'move'". **§6's own goal statement already reads "promotion **adds** a track" (`:2171`) and "The verb is `add`, not `move`" (`:2172`), and `:2185` already says "the verb is **ADD**, not move" -- so there is nothing to change in §6.** The one live occurrence in user-facing help, and the sole `move`-hit anywhere under `packages/shorebird_cli/lib/src`, is `packages/shorebird_cli/lib/src/commands/patches/set_track_command.dart:37` `'The deployment track to move the patch to '`. Change to "add the patch to". *Check:* `dart test packages/shorebird_cli` -- expect a golden/usage test to need updating, which is the point.
10. **Record why the device row is not in this order.** The chain, all verified: `compileShorebirdYaml` (`vendor/flutter/packages/flutter_tools/lib/src/shorebird/shorebird_yaml.dart:55`) copies only `base_url`, `auto_update`, `patch_verification` (`:68-70`) -- **never `channel`** -> the shipped yaml has no channel -> `vendor/updater/library/src/config.rs:145-148` falls back to `DEFAULT_CHANNEL` = `"stable"` (`:24`) -> `network.rs:301` `channel: config.channel.clone()` puts `"stable"` in `PatchCheckRequest.channel` (`:262`) -> the server resolves `str('channel') ?? 'stable'` (`api.dart:1953`, in `_patchesCheck` `:1934`). So an unmodified device on any release **always asks for `stable`**. Per §6's own retraction this makes the row **NOT VALIDATED, not blocked** (`PARITY.md:2193-2199`) -- reachable via the Dart API (step 14) or `shorebird preview --track` (`packages/shorebird_cli/lib/src/commands/preview_command.dart:97-100`).

### C · `G8` -- a fixture that drives updates

11. **[MUTATES nothing shared] Create the fixture, because it does not exist.** `selfhost/fixtures/` holds `airgap`, `airgap_app`, `evidence`, `CONTROL_PLANE_DATA.md` -- there is no manual-api app. Build `selfhost/fixtures/manual_api_app/`. This is the "happy accident" §16 names: a goal needing a new fixture **does not contend on `R6`** (`PARITY.md:2733-2735`), so it gets its own `app_id` and its own version counter and races nobody.
12. **Pin the package you are reasoning about, or you will reason about the wrong bytes.** `selfhost/fixtures/airgap_app/pubspec.lock:271-278` resolves `shorebird_code_push` **2.0.7 from `pub.dev`** as `dependency: transitive` -- pulled through `code_push_runtime` (`airgap_app/pubspec.yaml:29-30` path dep; `code_push_runtime/pubspec.yaml:29` `shorebird_code_push: ^2.0.0`) -- *not* from `vendor/updater/shorebird_code_push` (also 2.0.7, per its `pubspec.yaml:3`). Nothing overrides it: `dependency_overrides` appears in neither `airgap_app/pubspec.yaml` nor `packages/code_push_runtime/pubspec.yaml`. Because the dependency is transitive, the override must go in **your fixture's own** pubspec: `dependency_overrides: shorebird_code_push: {path: ../../../vendor/updater/shorebird_code_push}` -- **or** diff pub's copy against the vendored one and record the result. *Check:* `grep -A6 'shorebird_code_push:' <fixture>/pubspec.lock` shows `source: path`.
13. **Split the rows that are already exercised from the ones that are not.** `readCurrentPatch` is **already running on device on every airgap release**: `packages/code_push_runtime/lib/src/code_push_runtime.dart:202` is `(await updater.readCurrentPatch())?.number`. So §8's read surface has device evidence in `selfhost/evidence/releases/{24..32}/` -- but only where a screenshot shows a **number**: `_readPatch` catches every exception and returns `null` (`code_push_runtime.dart:203-206`), so `code patch: none` is ambiguous between "no patch" and "the call threw". Genuinely undriven: `checkForUpdate({UpdateTrack? track})` (`vendor/updater/shorebird_code_push/lib/src/shorebird_updater.dart:153`), `update({UpdateTrack? track})` (`:190`), `readNextPatch` (`:128`), and `auto_update: false` (documented at `packages/shorebird_cli/lib/src/commands/init_command.dart:383-387`). **Check before you claim -- do not credit the read rows to your own run.**
14. **Wire the track through, which is what unblocks `G6`.** The fixture's UI must call `checkForUpdate(track: UpdateTrack('beta'))`. The track reaches the native updater: `shorebird_updater_io.dart:73,95` -> `updater.dart:30-32` and `:37-39` marshal `track.name` over FFI -- legal because `UpdateTrack` is `extension type const UpdateTrack(String value)` (`shorebird_updater.dart:210`) with `String get name => value` (`:220-221`) -- and custom names are explicitly supported (`:196` doc, `:208` example, constants `:212-218`). This is the "other means" §6 says makes the device row reachable, and it is why queue item 10 (`PARITY.md:3216-3217`) puts the device row *after* `G8`.
15. **Explicitly copy the fixture conventions that have already bitten.** `shorebird.yaml` **must** be a declared flutter asset or the CLI refuses to build -- recorded at `selfhost/fixtures/airgap_app/pubspec.yaml:34-38` ("Caught by the first warm run of this fixture, 2026-08-07"). Also carry `version:` as the release-version source and bump it per run.
16. **Stop at the Android device gate; do not attempt iOS.** §8's restart-required row is a **KNOWN GAP** decided by the same once-per-process activation guard as §5 and §9 (`PARITY.md:2275`), tracked as `G15` (`:2337-2340`). What the API reports on iOS can be **wrong**, not merely unverified. Document that as a §15 obligation. The separate **iOS Route B manual update path** row (`:2274`) is not source-determined -- it is unrun for want of `R1` -- so it stays **NOT VALIDATED** and out of scope; running it needs `R1` and belongs to `G15`/`G8(ios)`.

### D · `G10.2` -- non-interactive, against *our* control plane

17. **Verify the claim an adversarial pass already killed once, before writing anything.** `.github/workflows/e2e.yaml` **does** drive the CLI end to end: the `patch` job's "🧪 Run Patch E2E Tests" step runs `./scripts/patch_e2e.sh` (`e2e.yaml:123-129`), and that script runs `shorebird init --force -v` (`:31`), `shorebird doctor --verbose` (`:35`), `shorebird release android` (`:51`), `shorebird preview` (`:60`, `:79`) and `shorebird patch android` (`:69`). The `cli` job runs `dart test integration_test` in `packages/shorebird_cli` (`e2e.yaml:197-198`), which shells out to `shorebird create`/`release android`/`patch android` (`integration_test/shorebird_cli_integration_test.dart:99,132,172`). **Do not write "no CI workflow invokes the CLI"** -- that is the exact correction killed at `PARITY.md:3296-3299`.
18. **State the gap that *is* real: none of it touches our control plane.** `e2e.yaml:60` and `:142` set `SHOREBIRD_HOSTED_URL` to `api-dev.shorebird.dev` / `api.shorebird.dev` -- **upstream**. No workflow in `.github/workflows/` exercises `packages/code_push_server`'s release/patch path with the CLI. That, not "no CI runs the CLI", is `G10.2`.
19. **The prompt machinery is already BUILT -- measure it rather than rebuild it, and know exactly when it fires.** All four prompts fail fast instead of hanging: `packages/shorebird_cli/lib/src/logging/shorebird_logger.dart` `confirm` `:56-57`, `chooseOne` `:65-72`, `prompt` `:85-91`, `promptAny` `:99-104`, each calling `_failIfNonInteractive` (`:135-141`), whose guard is `:136` `if (isInteractive && shorebirdEnv.canAcceptUserInput) return;`. So it throws **only** when at least one of these holds: stdout is not a TTY (`packages/shorebird_cli/lib/src/interactive_mode.dart:18-22` -- note the path is `lib/src/`, **not** `lib/src/logging/`, and `:19` short-circuits `false` on `--json`), stdin is not a TTY, `CI` is set, or `--json` (`shorebird_env.dart:292-293`, `isRunningOnCI` `:299`). The runner catches it at `shorebird_cli_command_runner.dart:297` and returns `ExitCode.usage` at `:315`, emitting `interactive_prompt_required` under `--json` (`json_output.dart:54`). **So `G10.2` is a harness, not a feature -- and a harness that must break at least one of those four conditions or it measures nothing.**
20. **One stale script comment to correct, and one that is still TRUE -- leave it alone.**
    * **LEAVE `selfhost/scripts/airgap_acceptance.sh:241-243` AS IT IS.** It says "without it the CLI prompts 'Which release would you like to patch?' and `--no-confirm` does not answer that prompt, so the stage hangs then fails (observed live)". The prompt is real (`packages/shorebird_cli/lib/src/release_chooser.dart:76` builds it; `logger.chooseOne` at `:92` and `:102` presents it; `:11` already documents `--release-version` as the escape hatch) -- and it **can still hang**, because that script is run from a terminal via `airgap_run.sh:20`, where stdout and stdin are both TTYs, `CI` is unset and `--json` is absent, so step 19's guard returns without throwing and `chooseOne` blocks on stdin. Rewriting this comment would replace a true statement with a false one. Add a clause naming *when* the guard does convert it to a fast `ExitCode.usage` (no TTY / CI / `--json`) if you want, but do not re-aim or delete it. This file is also `G13`'s harness (`PARITY.md:2452`) carrying an unrun iOS code-patch stage (`:2488`) -- comment-only, no executable line.
    * **`selfhost/scripts/accept_android_default.sh:125-126` is wrong today.** It says "NO `--no-confirm` either — this CLI does not have it on `patch`, hence `yes |`". `patch_command.dart:115-118` registers `addFlag('confirm', hide: true)`; `addFlag` is negatable by default, so `--no-confirm` parses. `release_command.dart:131-134` is the same shape; both read it at `patch_command.dart:215` / `release_command.dart:250`. That is also why `airgap_acceptance.sh:263,267,518,571,585,596` can pass `--no-confirm` to both commands. **Correct the comment only. Do NOT remove `yes |` at `:130`** -- this is `G4.2`'s Android-half harness (`R12`'s Linux constraint is cited from `:17-19` at `PARITY.md:2710`) whose device gate is still owed (`:3210-3215`), it runs on `hermes-vps`, and this order cannot verify a behavioral change to it. Removing `yes |` would also remove the `PIPESTATUS`/SIGPIPE-141 handling the same comment documents at `:127-128`.
21. **Report the dead flag rather than "fixing" it silently.** `CommonArguments.noConfirmArg` (`packages/shorebird_cli/lib/src/common_arguments.dart:177-183`) is defined and **never registered on any command and never read** -- the working flag comes from the negatable `confirm` flag in step 20. Either delete the orphan or register it; do not leave a reader believing the descriptor is what parses.
22. **[MUTATES CI config] Add a non-interactive harness against our control plane -- in two arms, because `--json` and "no TTY" are different conditions.** A script `selfhost/scripts/ci_noninteractive.sh` (does not exist yet -- building it is this step) that runs `release android` + `patch android` against `cps-android` with `SHOREBIRD_TOKEN` set, `--no-confirm`, `--release-version`, **stdout not a TTY** (`… > log.txt 2>&1`, never under `script`/`unbuffer`), and `CI` unset:
    * **arm 1, no `--json`** -- proves the TTY predicate itself (`interactive_mode.dart:20-21`) is what carries the run.
    * **arm 2, with `--json`** -- proves the machine envelope, and is the only arm where `interactive_prompt_required` (`json_output.dart:54`) can appear at all. A `--json`-only harness cannot distinguish "no TTY" from "`--json`" because `:19` short-circuits.
    *Check:* both arms exit 0; `grep -c interactive_prompt_required log.txt` = 0 in arm 2. Then run `shellcheck -x --source-path=SCRIPTDIR --severity=warning selfhost/scripts/ci_noninteractive.sh` by hand -- precondition 10 says CI will not.
23. **Then attack the token rows deliberately -- this is what `:2363` and `:2364` are.** `PARITY.md:2364` records "An empty `SHOREBIRD_TOKEN` can surface as a JSON `FormatException`", and `:2363` "Token/auth failure produces a useful error" is still NOT VALIDATED. Current code: `packages/shorebird_cli/lib/src/auth/auth.dart:350` `_loadCredentials()` -- an empty value trims to `''` (`:353`), misses the `sb_api_` prefix (`:357`), reaches `CiToken.fromBase64` (`:365`), and on `FormatException` (`:373`) now emits "Failed to parse SHOREBIRD_TOKEN. Expected an API key (sb_api_...) or a legacy CI token." (`:375-378`) **and rethrows** (`:380`). So the *message* names its cause; whether the process exit is useful is what your run decides. Run three arms: unset, empty, garbage.

## Precommitted outcomes

**No precommitted table exists in the docs for any of these four lanes.** §6/§7/§8/§10 carry retractions and headlines but no outcome matrix. The precommitted blocks that do exist -- `PARITY.md:813`, `:928`, `:1397`, `:1828`, `:3019`, `:3077-3125` (the three-gate mint batch) and `:3166-3188` (`G4.2`/`G4.3`) -- cover `G3.x`, `G4.x` and `G15` and none of these four. So these are new, written before the runs. Favourable-looking rows are marked ⚠ because those are the dangerous ones (`PARITY.md:3309-3335`).

| # | observation | means | decided where |
|---|---|---|---|
| 1 | ⚠ named `beta`/`staging` tests pass first try | §6's five NOT VALIDATED rows earn **BUILT**, nothing more. **The code path was already generic and already had one named channel (`internal`, `api_test.dart:2898`)** -- so this prices coverage, not capability. Do not read it as the device row moving | host / `R10` |
| 2 | a `beta` device-check returns stable's patch | supersession is **not** channel-scoped, contradicting `repository.dart:1205`. Highest-value failure here; stop and re-read before editing | host / `R10` |
| 3 | ⚠ `--track=beta` publishes and `check` returns it, and you conclude tracks work | **producer/host finding only.** Nothing about the device: step 10's chain shows a real device always asks for `stable`. Writing "tracks work" here is precisely §6's retracted overreach (`PARITY.md:2193-2199`) | host, mistakable for device |
| 4 | `rollout` grep finds a CLI/client/protocol surface after all | my step-8 negative grep was wrong -- **verify at the location** before believing either of us (`PARITY.md:3296-3299`) | host |
| 5 | the `G7` decision lands as "ship as-is, documented" | legitimate outcome. §7's rows stay **KNOWN GAP** / **NOT BUILT**; a written decision does **not** upgrade a status | design call |
| 6 | ⚠ `yaml.rs` default reads `Strict`, so "the default verifies nothing" looks wrong | it is right for a **different reason** -- `lifecycle.rs:797-803` skips on absent public key, and the production install path `:897-922` verifies nothing at all. Correcting the headline instead of the mechanism would launder a guess as a fix | host, source-determined |
| 7 | ⚠ the `code_signer_test.dart` suite is green | **it was green before you arrived** (`:47`,`:72`,`:97`,`:150`). This credits nothing to this order. The only new information would be a test crossing the Dart-PEM / Rust-base64-DER seam (step 4) | host, pre-existing |
| 8 | `checkForUpdate(track:)` returns `outdated` for a beta patch on the fixture | `G8`'s check row earns **BUILT** on host; **PROVEN** only once the update installs and the patched value renders on a device | host, then `R2` |
| 9 | ⚠ the manual-api fixture shows a patch number immediately | may be `readCurrentPatch` (**already** exercised by every airgap run, step 13) rather than your `update()` call. Assert the *number changed after* the call, not that it is non-null | fixture -- easy false green |
| 10 | non-interactive run exits 0 in both arms, log has no `interactive_prompt_required` | `G10.2`'s `:2361`/`:2362` rows earn **BUILT**. **PROVEN** needs the patch running on a device from that CI-shaped invocation | host |
| 11 | non-interactive run exits 64 naming a prompt | **the good failure.** The guard works and a real flag is missing; the message names which. Fix the flag, not the guard | host |
| 12 | non-interactive run **hangs** | the guard has a hole -- some prompt bypasses `ShorebirdLogger`, or the arm accidentally had a TTY. Check the redirect first, then find the raw `stdin` read. Higher-value than a pass | host |
| 13 | empty `SHOREBIRD_TOKEN` exits non-zero with the `:375-378` message | `:2363` earns **BUILT** and `:2364` is addressed. If it exits with a bare `FormatException` and no message, the KNOWN ISSUE stands verbatim | host |
| 14 | iOS manual-api row looks passable | **do not run it.** The restart-required half is determined from source (`PARITY.md:2275`, `G15` at `:2337-2340`) and the path half needs `R1`, which this order does not hold. A green run would be luck, and a red one would be misattributed | out of scope |

## Exit criteria

* **Earns BUILT** -- (A) `selfhost/SIGNING.md` exists, states the decision and its rejected alternatives, and cites `yaml.rs:7-8` + `lifecycle.rs:796-803` + `lifecycle.rs:897-922` + `updater_state.rs:347`; the pre-existing `code_signer_test.dart` is run and recorded **as pre-existing**. (B) `dart test -x integration` green with named `beta`/`staging` channels asserted independent, and `set_track_command.dart:37` says "add". (C) `selfhost/fixtures/manual_api_app/` builds, its lockfile resolves `shorebird_code_push` by `path`, and `checkForUpdate`/`update`/`auto_update:false` are exercised on host. (D) both harness arms complete `release android` + `patch android` against our control plane with stdout not a TTY, exit 0, zero `interactive_prompt_required` in the `--json` arm, `accept_android_default.sh:125-126` corrected, and `airgap_acceptance.sh:241-243` verified still-true and left in place.
* **Earns PROVEN** -- **nothing in this order does.** Every PROVEN row here needs hardware this order does not hold: `G6`'s device row (`R2` + the `G8` fixture), `G7`'s tamper rejection (`R1`/`R2`, and only in `Strict` mode at next boot), `G8`'s Android manual update (`R2`), `G10.2`'s "the CI-shaped invocation produced a patch that ran" (`R2`). A green suite, a compiling fixture, and a clean host harness are **BUILT** and no more -- all five disqualifiers at `PARITY.md:3254-3258` apply, and `:3261` says a host probe earns BUILT.
* **What is out of scope, and under which label** -- `G8`'s **restart-required** row is already **KNOWN GAP** (`PARITY.md:2275`), source-determined by `G15`'s once-per-process activation guard (`:2337-2340`); do not re-file it and do not book `R1` for it. `G8`'s **iOS manual update path** row (`:2274`) stays **NOT VALIDATED** -- it is unrun for want of `R1`, which is a scheduling fact, not a source-determined one. Neither is the `G15` second-engine situation, where no harness creating two engines exists at all and the row was relabelled **NOT BUILT** with "NOT RUNNABLE" as the reasoning (`PARITY.md:3121-3125`); "NOT RUNNABLE" is not a status in this file's vocabulary, so do not introduce it as one.

## Evidence to record

`selfhost/evidence/` currently holds only `releases/{24..32}/`. These lanes are host findings, not release cuts, so create sibling directories -- **they do not exist yet**:

* `selfhost/evidence/g6-tracks-server/` -- `test_output.txt` (full `dart test -x integration` run, with the named-track test names visible); `supersession.md` quoting `repository.dart:1205` and the per-track `check()` results; `rollout_surface.md` recording the CLI/client/protocol absence with the exact greps and their dates.
* `selfhost/SIGNING.md` -- the deliverable itself, not evidence. Alongside it, `selfhost/evidence/g7-signing/verification_path.md`: both gates with `yaml.rs:7-8`, `lifecycle.rs:678/686/771/796/802/897-922`, `updater_state.rs:347/363`, `signing.rs:37`, the retraction of §7's mechanism sentence, and an explicit note that `code_signer_test.dart`'s coverage predates this order.
* `selfhost/evidence/g8-manual-api/` -- `pubspec.lock.excerpt` proving the `path` resolution; `host_run.txt` for `checkForUpdate`/`update`; `restart_required.md` recording the `G15` dependency as a §15 documentation obligation.
* `selfhost/evidence/g10.2-noninteractive/` -- `ci_run_notty.log` and `ci_run_json.log` (stdout redirected, never a PTY, `CI` unset); `token_arms.txt` for unset/empty/garbage with exit codes; `flag_audit.md` on the negatable-`confirm` mechanism, the orphaned `noConfirmArg`, and the re-verification that `airgap_acceptance.sh:241-243` still describes real behavior.

**Identity facts to record in each** (`PARITY.md:3337-3344`): commit sha of the implementation; `code_push_server` version (`pubspec.yaml:6`, today `1.3.0`); `dart --version`; probe/script name; and for any device work that later follows -- release version, patch number, platform/device, engine hash. **Not** an engine hash or cell address for anything in this order: none of it is inside cell `4df8f9b6`, and writing one in would falsely couple these lanes to that mint.

## Commit shape

Four commits, one per lane, each self-contained. Never `-A`, never `commit -a` (`PARITY.md:2799-2801`). Because the index has been observed dirty with another worker's files, use the **pathspec form for all four** so the rest of the index cannot ride along.

Two things to know about the pathspec form before you use it: it commits the **working tree** content of those paths, ignoring the index for them; and `selfhost/PARITY.md` is a file another worker also edits. So before every commit that lists it, run `git diff -- selfhost/PARITY.md` and read it. If it contains prose you did not write, **stop** -- a pathspec commit would sweep their in-flight edits into your lane's commit, which is `9192a594`'s failure with a narrower blast radius (`PARITY.md:2833-2838`).

```
# A -- G7
git commit -m "docs(selfhost): G7 — the default verifies nothing, and not for the reason recorded" \
  -- selfhost/SIGNING.md selfhost/PARITY.md \
     selfhost/evidence/g7-signing/verification_path.md

# B -- G6 server half
git commit -m "test(selfhost): G6 — named tracks stay independent, and the verb is add" \
  -- packages/code_push_server/test/api_test.dart selfhost/PARITY.md \
     packages/shorebird_cli/lib/src/commands/patches/set_track_command.dart \
     packages/shorebird_cli/test/src/commands/patches/set_track_command_test.dart \
     selfhost/evidence/g6-tracks-server/

# C -- G8
git commit -m "feat(selfhost): G8 — a fixture that drives its own updates" \
  -- selfhost/fixtures/manual_api_app/ selfhost/PARITY.md \
     selfhost/evidence/g8-manual-api/

# D -- G10.2
git commit -m "feat(selfhost): G10.2 — the workflow runs with no TTY, against our own control plane" \
  -- selfhost/scripts/ci_noninteractive.sh \
     selfhost/scripts/accept_android_default.sh selfhost/PARITY.md \
     selfhost/evidence/g10.2-noninteractive/
```

Note commit A no longer carries a `code_signer_test.dart` path: that test already exists and this order does not author it. Note commit D no longer carries `airgap_acceptance.sh`: step 20 establishes its comment is still true.

**PARITY.md edits that must land in the SAME commit as their lane** (§17 Claims, `PARITY.md:2845-2846`):

* Claims row `:2858` -- take `R10` on commit B, with **TREE HEALTH: GREEN** and the format/analyze/test results from precondition 4; **clear it when you stop, even mid-goal** (`PARITY.md:2867-2868`).
* Commit A: rewrite `:2231`'s mechanism sentence keeping the old text visible as a retraction (`PARITY.md:3305-3307`); flip `:2236` `NOT BUILT A documented rotation procedure` only if `SIGNING.md` actually carries the procedure; leave `:2230`-`:2239`'s other rows alone.
* Commit B: `:2181`-`:2183` `NOT VALIDATED` -> `BUILT` **for the server rows only**; leave `:2184`'s device row `NOT VALIDATED` and say why (step 10). Reclassify `:2187` progressive rollout per step 8. **No "move" fix is needed in §6** -- `:2171`, `:2172` and `:2185` already say "add"/"ADD" (step 9).
* Commit C: `:2270`-`:2273`; mark `readCurrentPatch`'s already-exercised status separately from your own run (step 13); leave `:2274` and `:2275` untouched.
* Commit D: `:2361`-`:2362` -> `BUILT`; `:2363` and `:2364` per step 23's arms.
* Any lane: append one line to the queue's `Banked this session` table (`PARITY.md:3229-3238`).

PR titles are semantic-commit enforced in CI (`CLAUDE.md`; `.github/workflows/main.yaml:26-28`).

## Do not

* **Do not upgrade anything here to PROVEN.** Not for a green suite, not for a compiling fixture, not for a clean host harness -- the five disqualifiers are enumerated at `PARITY.md:3254-3258`.
* **Do not claim `code_signer_test.dart` as this order's work.** It is in the tree today with 20-plus tests including the sign→verify round trip (`:150`). Crediting an existing test to a new goal is the same category of error as crediting a host probe with PROVEN.
* **Do not rewrite `airgap_acceptance.sh:241-243`.** The prompt still hangs when that script is run the way it is run -- from a terminal, where `isInteractive` and `canAcceptUserInput` are both true. Replacing a true comment with a false one is worse than a stale row (`PARITY.md:3291-3293`).
* **Do not remove `yes |` from `accept_android_default.sh:130`.** That harness's `G4.2` Android device gate is unrun, it runs on `hermes-vps` (`R12`), and this order cannot verify a behavioral change to it. Correct the comment's false clause and stop.
* **Do not report a negative grep as coverage.** This is §6's own retracted mistake: `grep beta` returning nothing became "the server half is absent work" (`PARITY.md:2201-2204`, `:3300-3303`). It still returns nothing, and the server half is still largely done.
* **Do not infer a second fact from a verified first.** §6's other retraction: a real `channel` omission became "permanently stable-only" (`PARITY.md:2193-2199`). The omission is real (step 10); "blocked" was not.
* **Do not confuse `code_push_server/lib/src/signing.dart` (`UrlSigner`, HMAC download-URL signing) with patch signing.** The server has no RSA library at all (`PARITY.md:2257-2259`).
* **Do not staff `G7` as a lane.** §16 is explicit that it does not make a fifth lane -- one CLI test plus `SIGNING.md`, and the test is already written (`PARITY.md:2777-2778`).
* **Do not `git stash`, `git restore .`, `git checkout .`, or switch branches** (`PARITY.md:2802-2805`). A live worker landed `ba4e1c02` within the hour.
* **Do not touch `selfhost/fixtures/airgap_app/`.** That is `R6`, the sharpest serializer (`PARITY.md:2704`, and why at `:2712-2731`). Your fixture is a new directory; that is the whole point.
* **Do not edit an executable line of `airgap_acceptance.sh` or `accept_android_default.sh`.** They are `G13`'s and `G4.2`-Android's harnesses with unrun gates (`PARITY.md:2488`, `:3210-3215`), and they are not R-ids, so nothing on the claims table will warn the other worker.
* **Do not book `R1` for `G8`'s iOS rows or `G6`'s device row from inside this order.** One iPhone goal at a time (`PARITY.md:2739-2740`), and the mint/device lane is live.
* **Do not run the server suite from the repo root.** It is not a workspace member (root `pubspec.yaml:5-18`) and has its own lockfile; CI runs it with `working-directory: packages/code_push_server` (`code_push_server.yaml:23`).
* **Do not run the non-interactive harness under a PTY wrapper, and do not let `--json` do the work.** `script`, `unbuffer`, or a `tmux` pane makes `isInteractive` true via either `stdout.hasTerminal` or the `stdioType` fallback (`interactive_mode.dart:18-22`), and a `--json`-only arm returns `false` at `:19` regardless of the TTY -- so a single `--json` arm cannot tell you the redirect worked. Both vacuities are the same class as the `+0.00 %` arm (`PARITY.md:3317-3321`).
* **Do not "fix" another worker's stale claim row.** `R3`/`R11` at `:2852`/`:2859` still reference `ee001fd7` after `ba4e1c02` minted `4df8f9b6`. Report it; do not edit it.

## Open questions

1. **Which `G7` option ships.** (ii) "loud opt-in" is recommended, but a fork whose Android and iOS legs are PROVEN unsigned may prefer (i) with a blunt `SIGNING.md`. Tradeoff: a warning is one test and some noise on every release; silence is free and leaves a self-hoster with a false sense of integrity. **Decide in step 1, before writing code.**
2. **Whether the Dart-to-Rust key-encoding seam is worth a test.** `CodeSigner.verify` takes a PEM (`code_signer.dart:151`); the updater's `ring` verifier takes base64 DER from `base64PublicKey` (`:56-61`). Nothing crosses that boundary in a test today. Cheap option: a golden asserting `base64PublicKey`'s output is exactly what `signing.rs`'s decoder accepts. Expensive option: an FFI round trip. Neither is required for §7's rows.
3. **Add `rollout` to the CLI/protocol, or document the gap.** Adding it means a protocol change across `shorebird_code_push_protocol` + `shorebird_code_push_client` + `patch_command`, and diverging from upstream's message shape. Documenting it leaves progressive rollout admin-only forever. §7's KMS row (`PARITY.md:2239`) is the precedent for folding an aspiration into an existing mechanism rather than standing it up as its own obligation.
4. **Whether `G10.2`'s harness becomes a GitHub workflow or stays a script.** A workflow needs a reachable control plane from a runner -- our instances are `:18080`/`:18081` on this Mac. A script run by the acceptance harness is honest and available today; a workflow is what "CI parity" literally names. Cheapest first: script now, note the workflow as owed. Note also that a new `selfhost/scripts/*.sh` is unlinted by any workflow today (precondition 10), which is a small argument for the workflow.
5. **Whether §17's one-worktree premise gets rewritten.** `git worktree list` returns four entries; three are detached study checkouts. Rewriting the rule risks implying isolation that the study trees do not provide. Leaving it risks a worker concluding the shared-tree rules do not apply. Recommend: correct the *fact* at `:2793-2795`, keep the *rule*.